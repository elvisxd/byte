# El contexto de build no necesita nada de esto (y .venv/ y .git/ pesan).
.venv/
venv/
.git/
.github/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.env
.env.*
!.env.example
tests/
docs/
evals/
n8n/
cli/
node_modules/
*.md

# Lo privado del perfil no entra en ninguna imagen. `*.md` ya se excluye, pero
# `privado.toml` no lo cubría y una imagen se publica.
perfil/privado.*
