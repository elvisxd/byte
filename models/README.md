# models/
Esquemas de datos (Pydantic / SQLAlchemy). Ver "Base de datos (ERD)" en `docs/plan-asistente-ia-local.md`.

- `schemas.py` — esquemas de la API: `Conversation`, `Message`, `RunAccepted`,
  `RunState`, `HealthDetails` y los cuerpos de cada request.
